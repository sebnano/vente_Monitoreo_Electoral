<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ElectoralMonitoring.ScannerPreviewPage"
             Title="{x:Static res:AppRes.ScannerPreviewPageTitle}"
             x:DataType="local:ScannerPreviewPageModel"
             xmlns:res="clr-namespace:ElectoralMonitoring.Resources.Lang"
             xmlns:local="clr-namespace:ElectoralMonitoring">
    <ScrollView>
        <StackLayout Spacing="25">
            <Grid ColumnDefinitions="Auto,*" Margin="10" ColumnSpacing="10">
                
            <Border Grid.Column="0" HorizontalOptions="Start" >
                <Image Source="{Binding ImagePreview}" Aspect="AspectFit"
                       HeightRequest="200"></Image>
            </Border>
                <StackLayout Grid.Column="1" Spacing="6">
                    <Label Text="Acta escaneada" FontSize="Title" FontAttributes="Bold"/>
                    <Label Text="Confirme los datos y presione Aceptar" FontSize="Subtitle"/>
                    <Label Text="Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book. It has survived not only five centuries, but also the leap into electronic typesetting, remaining essentially unchanged." FontSize="Caption"/>
                </StackLayout>
            </Grid>
            <StackLayout VerticalOptions="FillAndExpand"></StackLayout>
            <Border VerticalOptions="End" Padding="0" StrokeThickness="0" Background="{StaticResource Primary}" StrokeShape="RoundRectangle 20,20,0,0">
                <Button Text="{x:Static res:AppRes.ConfirmScanBtn}" HeightRequest="{StaticResource BottomButtonHeight}" FontSize="Title"/>
            </Border>
        </StackLayout>
    </ScrollView>
</ContentPage>
